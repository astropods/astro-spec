# astro-spec

Parser and type definitions for the Astro AI agent specification
(`astropods.yml`).

This repo is normally checked out as `astropods/astro`'s
`packages/astro-spec` submodule. That repo's `agents.md` (Writing style,
Development Workflow) applies here too; where this file says something
different, this file wins.
